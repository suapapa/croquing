# Optional: call the backend directly (requires matching CORS_ORIGINS).
# Leave unset to use same-origin /api via the Vite dev proxy (recommended).
# VITE_API_BASE=http://localhost:8080

# Vite dev proxy target (docker-compose sets this to http://backend:8080).
# VITE_API_PROXY_TARGET=http://localhost:8080
